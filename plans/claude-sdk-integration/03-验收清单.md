# 验收清单

## 1. 自动化

- [x] `npm run typecheck`
- [x] `npm test`
- [x] `npm run build`

## 2. OpenClaw 回归

- [x] 默认 `WE_CLAW_RUNTIME` 未设置时仍连接 OpenClaw
- [x] `/api/gateway/ws` 兼容路径仍可用
- [x] `/api/runtime/ws` 在 OpenClaw 模式下可复用 Gateway bridge

## 3. Claude Agent SDK 手动验收

- [x] 使用 `WE_CLAW_RUNTIME=claude-agent-sdk` 启动
- [x] 使用只读工具配置启动：`WE_CLAW_CLAUDE_SDK_ALLOWED_TOOLS=Read,Glob,Grep`
- [x] `sessions.list` 能列出当前 cwd 的 Claude session
- [x] 创建新工作后发送 prompt
- [x] 能看到流式文本输出
- [x] 能捕获真实 Claude `session_id`
- [x] 刷新 session list 后能看到持久 session
- [x] 切换历史 session 后能加载 history
- [x] 长任务可请求停止，UI running 状态能结束

## 4. 已知缺口

- inline approval 首期未完成，默认用 `dontAsk` 避免阻塞。
- 本机 Claude E2E 以已配置的 Claude Code 登录态为准，不强制 `ANTHROPIC_API_KEY`；已用 SDK `query()` 验证同一用户环境可复用本机 OAuth 认证并得到真实模型响应。
- 已验证 `http://127.0.0.1:4183/api/bootstrap` 能返回 `runtime.kind=claude-agent-sdk`、`version=0.2.123`、`bridgePath=/api/runtime/ws`。
- 已验证 `ws://127.0.0.1:4183/api/runtime/ws` 可完成 `connect`、`health`、`sessions.subscribe`、`sessions.messages.subscribe` 请求。
- 已验证 `WE_CLAW_CLAUDE_SDK_ALLOWED_TOOLS=Read,Glob,Grep` 下 `chat.send` 可通过真实 SDK query 返回 `delta=ok` / `final=ok`，并捕获真实 `session_id=922a9cab-1f42-470f-81c2-7aaa2c1bb07c`。
- 已验证 `chat.history` 可用真实 `claude:<sessionId>` 读取持久历史，返回 user/assistant 两条消息。
- 已验证 `chat.abort` 会立即返回 `{ status: "aborted" }`，并让 session list 中对应工作脱离 `running`。
- 已验证默认 OpenClaw 模式下 `/api/bootstrap` 返回 `runtime.kind=openclaw`，`/api/gateway/ws` 与 `/api/runtime/ws` 都能收到 Gateway `connect.challenge`。
- 已补自动化覆盖：Claude history 中的 `tool_use` / `tool_result` 不再在刷新后消失，compact boundary 会转成 notice。
- 已补实现：Claude SDK 仅在 runtime 方法实际需要时动态加载；OpenClaw 默认路径不再顶层 import SDK。
