# 验收清单

## 1. 自动化

- [x] `npm run typecheck`
- [x] `npm test`
- [x] `npm run build`

## 2. OpenClaw 回归

- [ ] 默认 `WE_CLAW_RUNTIME` 未设置时仍连接 OpenClaw
- [ ] `/api/gateway/ws` 兼容路径仍可用
- [ ] `/api/runtime/ws` 在 OpenClaw 模式下可复用 Gateway bridge

## 3. Claude Agent SDK 手动验收

- [x] 使用 `WE_CLAW_RUNTIME=claude-agent-sdk` 启动
- [ ] 使用只读工具配置启动：`WE_CLAW_CLAUDE_SDK_ALLOWED_TOOLS=Read,Glob,Grep`
- [ ] `sessions.list` 能列出当前 cwd 的 Claude session
- [ ] 创建新工作后发送 prompt
- [ ] 能看到流式文本输出
- [ ] 能捕获真实 Claude `session_id`
- [ ] 刷新 session list 后能看到持久 session
- [ ] 切换历史 session 后能加载 history
- [ ] 长任务可请求停止，UI running 状态能结束

## 4. 已知缺口

- inline approval 首期未完成，默认用 `dontAsk` 避免阻塞。
- 未在本清单中要求真实 Claude API 调用通过；需要本机具备 Anthropic API key 或受支持云供应商认证。
- 已验证 `http://127.0.0.1:4183/api/bootstrap` 能返回 `runtime.kind=claude-agent-sdk`、`version=0.2.123`、`bridgePath=/api/runtime/ws`。
- 已验证 `ws://127.0.0.1:4183/api/runtime/ws` 可完成 `connect` 和 `health` 请求。
