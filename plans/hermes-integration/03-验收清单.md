# 验收清单

## 1. 自动化

- [x] `npm run typecheck`
- [x] `npm test`
- [x] `npm run build`

## 2. OpenClaw / Claude 回归

- [x] 默认 `WE_CLAW_RUNTIME` 未设置且无 persisted runtime settings 时仍连接 OpenClaw（由 `test/launcher.test.ts` 覆盖；当前本地 `.runtime/settings.json` 选择 Claude SDK）
- [x] `/api/gateway/ws` 兼容路径仍可用（OpenClaw bootstrap 仍暴露 `/api/gateway/ws`）
- [x] `/api/runtime/ws` 在 OpenClaw 模式下可复用 Gateway bridge（`WE_CLAW_RUNTIME=openclaw` bootstrap 验证通过）
- [x] `WE_CLAW_RUNTIME=claude-agent-sdk` 仍可完成 `sessions.list`（本地 `/api/runtime/ws` 验证返回 15 条 session）
- [~] Claude Agent SDK runtime 仍可完成 `chat.send`、stream delta/final、`chat.history` 和 `chat.abort`（自动化覆盖既有 adapter normalizers；未手动触发真实模型 run）

## 3. Hermes 手动验收

- [x] 使用 `WE_CLAW_RUNTIME=hermes` 启动（launcher 可启动；Hermes runtime 通过自动探测的 `/Users/insunny/Documents/codespace/hermes-agent/venv/bin/python` 标记 available/ready）
- [x] 使用显式 Hermes 配置启动：`WE_CLAW_HERMES_ROOT`、`WE_CLAW_HERMES_CWD`、`WE_CLAW_HERMES_PYTHON`（显式 root/cwd 验证到 bootstrap；Hermes venv 中 `dotenv` 与 `tui_gateway.entry` import 均通过）
- [x] `/api/bootstrap` 返回 `runtime.kind=hermes`、`transport=stdio-jsonrpc`、`bridgePath=/api/runtime/ws`
- [x] `sessions.list` 能列出 Hermes 持久 session（真实 adapter 与 `/api/runtime/ws` 验证通过）
- [x] 创建新工作后能捕获 Hermes active `session_id`（真实 adapter 与 `/api/runtime/ws` 验证通过，并生成 `hermes:<persistedSessionId>`）
- [x] 新工作在 UI 中使用 `hermes:<persistedSessionId>` 作为 session key（adapter/session normalizer 单元测试覆盖）
- [x] 发送 prompt 后能看到 `message.delta` 或 `message.complete` 输出到 conversation（真实 adapter 与 `/api/runtime/ws` 订阅验证通过）
- [x] `message.complete` 后 UI running 状态结束（Hermes complete -> existing `final` reducer shape 单元测试覆盖）
- [x] 刷新 session list 后能看到 Hermes 持久 session（真实 adapter 与 `/api/runtime/ws` 验证通过）
- [x] 切换历史 session 时能先 `session.resume`，再加载 `session.history`（单元测试覆盖）
- [x] 历史 session resume 后继续发送 prompt 使用 active Hermes `session_id`（单元测试覆盖）
- [x] 长任务可请求停止，`chat.abort` 能调用 `session.interrupt` 并结束 UI running 状态（单元测试覆盖方法翻译）
- [x] Hermes stderr / 启动失败 / protocol error 能显示为 diagnostic，而不是静默断连（bootstrap diagnostic 与 JSON-RPC stderr/startup timeout 单元测试覆盖；本机缺失 `dotenv` diagnostic 已手动验证）

## 4. 已知缺口

- inline approval 首期未完成；`approval.request` 只做 unsupported / partial 能力提示或轻量 notice。
- clarify / sudo / secret 首期未完成；如果 Hermes run 等待输入，UI 需要明确显示等待状态，避免看起来像卡死。
- Hermes tool activity 首期只要求 compact row 或 notice，不要求完整结构化参数、结果和耗时。
- Hermes history 中纯 tool call、image attach 或 metadata-only message 仍没有完整结构化还原；首期可能显示为 unsupported content 或通过 tool row/notice 降级。
- Hermes Dashboard / FastAPI `/api/ws` 不作为首期验收路径；首期只验收 `python -m tui_gateway.entry` 的 stdio JSON-RPC adapter。
- Hermes stdio client 当前按 Hermes TUI Gateway 的 newline-delimited JSON-RPC 约定实现，不支持 LSP `Content-Length` framing；真实 Hermes lifecycle 验证需要确认该 wire format 没有漂移。
- Hermes 有 persisted session id 与 active runtime session id 两层语义；验收必须覆盖历史 resume 后继续发送和中断，避免只验证新建会话 happy path。
- `WE_CLAW_HERMES_ROOT`、Python virtualenv 和本机依赖会影响真实 E2E；We-Claw 现在未显式设置 `WE_CLAW_HERMES_PYTHON` 时会优先探测 Hermes checkout 下的 `venv/bin/python` / `.venv/bin/python`，并在启动快照中展示实际 Python。

## 5. 2026-05-01 验收记录

- 已运行 `/Users/insunny/Documents/codespace/hermes-agent/setup-hermes.sh` 创建 Hermes 本地 `venv`；`python-dotenv` 已安装，`import dotenv, tui_gateway.entry` 通过。
- `WE_CLAW_RUNTIME=hermes` + `WE_CLAW_HERMES_ROOT=/Users/insunny/Documents/codespace/hermes-agent` 下，`/api/bootstrap` 返回 `runtime.kind=hermes`、`transport=stdio-jsonrpc`、`available=true`、`ready=true`、`diagnostics=[]`。
- 真实 adapter 验证：`sessions.list`、`sessions.create`、`chat.history`、`chat.send`、stream delta/final、`chat.abort`、再次 `chat.history`、再次 `sessions.list` 均通过。
- 真实 `/api/runtime/ws` 验证：订阅 `sessions.messages.subscribe` 后发送 prompt，收到 delta 文本与 final 文本；history 中返回 2 条消息。
- 真实浏览器 UI E2E 验证：未设置 `WE_CLAW_RUNTIME`、仅配置 Hermes root/cwd 启动；在设置面板点击 Hermes 后状态变为 `Hermes Connected`，新建工作并发送 prompt 后 conversation 收到 Hermes assistant 回复 `we-claw browser e2e ok`。截图保存于 `.tmp-screens/hermes-ui-e2e-settings-before.png`、`.tmp-screens/hermes-ui-e2e-settings-after.png`、`.tmp-screens/hermes-ui-e2e-reply.png`。
